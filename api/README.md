# api

Shared Dart package containing Protobuf schema definitions and generated gRPC code. Both `server/` and `client/` depend on this package.

Schemas live in `lib/spec/`, generated Dart files in `lib/build/`. Use the **API: Regenerate APIs** VS Code task after editing `.proto` files.
